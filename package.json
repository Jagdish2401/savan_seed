{
  "name": "final_project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix backend run start",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "seed:hr": "npm --prefix backend run seed:hr"
  }
}
